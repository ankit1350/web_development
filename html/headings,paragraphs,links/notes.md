**lorem20**= 'used to get template for specific word count', used mainly for placeholders.

~ `<!-- and -->`  use this for comment

~ Literal character	Character reference equivalent
   * <	               -              ` &lt;`
   * >	               -             	`&gt;`
   * "	               -             `	&quot;`
   * '	               -             `	&apos;`
   * &	               -             `	&amp;`

   `<em>` for *italic*
   `<strong>` for **bold** statement