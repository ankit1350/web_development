# SEO related
* **LightHouse** on any web page shows improvement which can be done on the site etc for better
user performance.
* always give width and height to prevent layout shift on the site
* meta desc : short description about the site  shown on search engine, just below the title
```html
<img src="img.webp" loading="lazy">
```
### Browser loads image only when it’s about to appear
* Faster initial load
* Less bandwidth usage
* Better Core Web Vitals

| Tag            | Purpose                                                         |
| -------------- | --------------------------------------------------------------- |
| `<header>`     | Introductory content or navigation links at the top.            |
| `<nav>`        | Navigation links section.                                       |
| `<main>`       | Main content of the page (unique for each page).                |
| `<section>`    | A thematic grouping of content (like a chapter).                |
| `<article>`    | Independent, self-contained content (blog post, news article).  |
| `<aside>`      | Sidebar content, related but not part of the main flow.         |
| `<footer>`     | Footer section with copyright, links, etc.                      |
| `<figure>`     | Represents media (image, chart, diagram) with optional caption. |
| `<figcaption>` | Caption for `<figure>`.                                         |
| `<mark>`       | Highlighted text (important).                                   |
| `<time>`       | Date or time information.                                       |
| `<address>`    | Contact information of author/owner.                            |
